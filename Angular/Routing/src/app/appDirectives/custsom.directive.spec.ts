import { CustsomDirective } from './custsom.directive';

describe('CustsomDirective', () => {
  it('should create an instance', () => {
    const directive = new CustsomDirective();
    expect(directive).toBeTruthy();
  });
});
