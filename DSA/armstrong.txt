// Online C++ compiler to run C++ program online
#include <iostream>
bool isArmstrong(int n){
    int original = n;
    int sum = 0;
    while(n != 0){
        int digit = n % 10;
        sum += (digit * digit * digit);
        n = n /10;
    }
    return sum == original;
}
int main() {
    // Write C++ code here
    int n = 1533;
    std::cout << isArmstrong(n)<<std::endl;

    return 0;
}